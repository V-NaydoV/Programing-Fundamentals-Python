number = int(input())
shell_counter = 1
shell_list = []
while True:
    shell = 2 * (shell_counter ** 2)
    if number >= shell:
        number -= shell
        shell_list.append(shell)
        shell_counter += 1
    else:
        if number > 0:
            shell_list.append(number)
            break
        else:
            break
print(shell_list)


